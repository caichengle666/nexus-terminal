{
  "name": "nexus-electron-app",
  "version": "0.9.22.22",
  "releaseVersion": "0.9.22.22",
  "description": "Electron application for Nexus Terminal",
  "main": "main.js",
  "author": {
    "name": "caichengle666"
  },
  "homepage": "https://github.com/caichengle666/nexus-terminal",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/caichengle666/nexus-terminal.git"
  },
  "license": "ISC",
  "optionalDependencies": {
    "dmg-license": "^1.0.10"
  },
  "dependencies": {
    "electron-store": "^11.0.2",
    "express": "^5.2.1",
    "http-proxy-middleware": "^4.2.0",
    "iconv-lite": "^0.7.3",
    "node-pty": "^1.1.0"
  },
  "scripts": {
    "start": "electron .",
    "build:backend": "cd ../packages/backend && npm run build:prod",
    "build:frontend": "cd ../packages/frontend && npm run build",
    "prepare:node-runtime": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"New-Item -ItemType Directory -Force -Path 'nodejs-runtime/win-x64' | Out-Null; Copy-Item -Path (Get-Command node).Source -Destination 'nodejs-runtime/win-x64/node.exe' -Force\"",
    "build:electron": "npm run prepare:node-runtime && electron-builder --win",
    "build": "npm run build:frontend && npm run build:backend && npm run build:electron"
  },
  "devDependencies": {
    "electron": "^43.1.1",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "com.caichengle666.nexus-terminal",
    "productName": "Nexus Terminal",
    "buildVersion": "0.9.22.22",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "main.js",
      "preload.js",
      "updater-service.js",
      "update-network.js",
      "update-download-service.js",
      "update-verification.js",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "build/icon.ico",
        "to": "icon.ico"
      },
      {
        "from": "build/icon.png",
        "to": "icon.png"
      },
      {
        "from": "../packages/backend",
        "to": "packages/backend",
        "filter": [
          "dist/**",
          "html-presets/**"
        ]
      },
      {
        "from": "../packages/frontend/dist",
        "to": "packages/frontend/dist"
      },
      {
        "from": "nodejs-runtime",
        "to": "nodejs-runtime"
      }
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "build/icon.ico",
      "artifactName": "${productName}-0.9.22.22-${name}.${ext}"
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "icon": "build/icon.png",
      "category": "public.app-category.developer-tools",
      "artifactName": "${productName}-0.9.22.22-macOS-${arch}.${ext}"
    },
    "nsis": {
      "artifactName": "${productName} Setup 0.9.22.22.${ext}",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "installerIcon": "build/icon.ico"
    },
    "includePdb": false
  }
}
